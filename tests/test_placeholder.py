"""Minimal test so pytest collects at least one test (avoids exit code 5)."""


def test_placeholder() -> None:
    """Placeholder so task test:run succeeds when no other tests exist."""
    assert True
